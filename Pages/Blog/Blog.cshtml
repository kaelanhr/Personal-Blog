@page "/Blog/{postTitle?}"
@model PersonalBlog.Pages.BlogModel

@{
	ViewData["Title"] = Model.Blog.Title;
	ViewBag.Title = Model.Blog.Title;
	ViewBag.Keywords = string.Join(",", Model.Blog.Tags);
	ViewBag.Description = Model.Blog.Blurb;
}

@section Header{
	<section class="nav-background">
	</section>
}

<article>
	<section class="blog-body-container">
		<section class="blog-body">
			<section class="blog-info">
				<section class="blog-title">
					<h1>@Html.DisplayFor(model => model.Blog.Title)</h1>
				</section>
				<section class="blog-details">

					<img src="/Images/portrait.jpg" id="user-icon" alt="User Icon" />
					<p>
						By Kaelan Reece | Published on @Model.Blog.Published.ToString("MMMM dd, yyyy") | @Model.Blog.Length
						min read
					</p>
				</section>
				<p>@Model.Blog.Blurb</p>
			</section>
			<img src="/Images/Blogs/@Model.Blog.Path/header.png" class="blog-header-image" />

			@Html.Raw(Model.Blog.Content)

			<hr>

			@if (!String.IsNullOrWhiteSpace(Model.Blog.ProjectUrl))
			{
				<a href="@Model.Blog.ProjectUrl">Project Url</a>
			}
			<section>
				<img src="/Images/portrait.jpg" id="user-icon" alt="User Icon" />
				<span>Kaelan Reece</span>
				<span>Software Engineer</span>
				<span>Tags: </span>
				@foreach (var tag in Model.Blog.Tags)
				{
					<span class="blog-tag">@tag</span>
				}
			</section>
		</section>
	</section>
</article>